FROM balenalib/raspberrypi3-python:3.7.4
RUN apt update && apt install -y wireless-tools libraspberrypi-bin iputils-ping && rm -rf /var/lib/apt/lists/* && usermod -a -G video root
ADD requirements.txt /
RUN pip3 install -r /requirements.txt
ADD common.py /