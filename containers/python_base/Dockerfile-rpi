FROM balenalib/raspberry-pi-alpine
ARG commit
RUN apk add --no-cache --virtual .build-deps python3 gcc musl-dev && echo -n $commit > /commit.txt
COPY requirements.txt smartcarsocket.py /
RUN pip install -r /requirements.txt
