FROM jrcichra/smartcar_python_base_rpi
ARG commit
EXPOSE 8080
COPY . /
RUN pip3 install -r /requirements.txt && apt install -y openssh-client sshpass && echo -n $commit > /commit.txt
CMD python3 -u transfer.py